# Required for the locally runnable Python execution core.
numpy>=1.21
pandas>=1.3

# File-format engines are intentionally optional and fail clearly when absent:
# openpyxl>=3.0        # XLSX
# pyarrow>=8          # Parquet / Feather
# Optional schema tooling (runtime validation does not depend on it):
# jsonschema>=4
